const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "db4free.net",
      user: "ladygaga",
      password: "55555555",
      database: "testladygaga",
    },
    listPerPage: 10,
  };
  module.exports = config;